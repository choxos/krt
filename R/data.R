#' An example Key Resources Table
#'
#' A small [krt_tbl] spanning several common resource types (antibody, cell
#' line, software, dataset, organism/strain, and protocol). Used throughout the
#' documentation and tests so that examples run offline.
#'
#' @format An object of class `krt_tbl` with six resources.
#' @source Constructed by `data-raw/06-build-example-krt.R`. Identifiers are
#'   real, publicly resolvable examples; the study is fictional.
#' @examples
#' krt_example
#' summary(krt_example)
"krt_example"

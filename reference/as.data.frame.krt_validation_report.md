# Convert a validation report to a data frame

Convert a validation report to a data frame

## Usage

``` r
# S3 method for class 'krt_validation_report'
as.data.frame(x, ...)
```

## Arguments

- x:

  A `krt_validation_report`.

- ...:

  Ignored.

## Value

A data frame with one row per finding.

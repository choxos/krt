% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{render_krt}
\alias{render_krt}
\title{Render a KRT as a formatted table}
\usage{
render_krt(
  x,
  path = NULL,
  format = c("md", "html", "docx"),
  profile = NULL,
  template = NULL,
  audience = c("author", "public"),
  redact = NULL
)
}
\arguments{
\item{x}{A \link{krt_tbl}.}

\item{path}{Output path, or \code{NULL} to return the text (md/html).}

\item{format}{\code{"md"}, \code{"html"}, or \code{"docx"}.}

\item{profile}{Profile controlling the layout (default the table's profile).}

\item{template}{Unused placeholder for a future Word template.}

\item{audience}{\code{"author"} (full) or \code{"public"} (redacted) for shared tables.}

\item{redact}{Redaction strength for public output, or \code{FALSE} to disable.}
}
\value{
The rendered text (md/html), or the path (invisibly) for docx.
}
\description{
Produces a human-readable Key Resources Table. The \code{"star-methods"} profile
projects the table to the three Cell Press columns (REAGENT or RESOURCE,
SOURCE, IDENTIFIER) and groups resources under the twelve standard STAR
Methods category headers, in the order the template uses. The \code{"generic"}
profile (the default) renders the ASAP six-column layout; any other named
profile renders through its own declared columns.
}
\examples{
cat(substr(render_krt(krt_example, profile = "star-methods"), 1, 80))
}

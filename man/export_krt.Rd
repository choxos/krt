% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_krt}
\alias{export_krt}
\alias{krt_write}
\title{Export a Key Resources Table}
\usage{
export_krt(
  x,
  path = NULL,
  format = c("json", "yaml", "csv", "tsv", "xlsx", "asap", "ris", "bibtex"),
  profile = NULL,
  audience = c("author", "public"),
  redact = NULL,
  attribution = TRUE,
  template = NULL,
  view = NULL
)

krt_write(
  x,
  path = NULL,
  format = c("json", "yaml", "csv", "tsv", "xlsx", "asap", "ris", "bibtex"),
  profile = NULL,
  audience = c("author", "public"),
  redact = NULL,
  attribution = TRUE,
  template = NULL,
  view = NULL
)
}
\arguments{
\item{x}{A \link{krt_tbl}.}

\item{path}{Output file path, or \code{NULL} to return the content as a string.}

\item{format}{Output format; inferred from \code{path} when possible.}

\item{profile}{Profile whose columns tabular exports use (default the table's
profile).}

\item{audience}{\code{"author"} (full) or \code{"public"} (redacted).}

\item{redact}{Redaction strength (\code{"basic"}/\code{"strict"}) for public exports,
or \code{FALSE} to disable (with a warning).}

\item{attribution}{If \code{TRUE} (default) and the profile carries an attribution
requirement, write an attribution sidecar next to \code{path}.}

\item{template}{Optional template path for ASAP/xlsx export.}

\item{view}{Optional explicit view for tabular formats.}
}
\value{
The path (invisibly) when written, otherwise the content string.
}
\description{
Writes a table in a chosen format. \code{"json"} and \code{"yaml"} are lossless;
\code{"csv"}, \code{"tsv"}, \code{"xlsx"}, \code{"asap"}, \code{"ris"}, and \code{"bibtex"} are lossy views
and raise a \code{lossy-export} warning listing fields that are not preserved as
columns. When \code{audience = "public"}, sensitive ethics fields are redacted by
default.
}
\examples{
export_krt(krt_example, format = "json") |> substr(1, 30)
suppressWarnings(export_krt(krt_example, format = "asap")) |> substr(1, 40)
}

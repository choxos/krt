% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_asap.R
\name{export_asap}
\alias{export_asap}
\title{Export a KRT in the ASAP six-column format}
\usage{
export_asap(
  x,
  path = NULL,
  template = NULL,
  format = NULL,
  audience = c("author", "public"),
  redact = NULL,
  attribution = TRUE
)
}
\arguments{
\item{x}{A \link{krt_tbl}.}

\item{path}{Output path, or \code{NULL} to return CSV text.}

\item{template}{Optional xlsx template path. When supplied, output is xlsx.}

\item{format}{\code{"csv"} or \code{"xlsx"}; inferred from \code{path}/\code{template}.}

\item{audience}{\code{"author"} (full) or \code{"public"} (redacted).}

\item{redact}{Redaction strength for public output, or \code{FALSE} to disable.}

\item{attribution}{If \code{TRUE} (default) and a \code{path} is given, write the ASAP
CC BY 4.0 attribution block as a sidecar next to it.}
}
\value{
The path (invisibly) when written, or CSV text.
}
\description{
Projects the table to the ASAP columns and writes it as CSV or into an xlsx
template. If \code{template} is supplied (or the bundled ASAP template is used),
the data is written into its \code{KRT} sheet, preserving its dropdowns and
attribution worksheet.
}
\examples{
cat(substr(export_asap(krt_example), 1, 60))
}

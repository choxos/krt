% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve.R
\name{resolvers}
\alias{resolvers}
\alias{resolve_rrid}
\alias{resolve_doi}
\alias{resolve_orcid}
\alias{resolve_pubmed}
\alias{resolve_ror}
\alias{resolve_cellosaurus}
\title{Resolve identifiers against public registries}
\usage{
resolve_rrid(rrid, resolve = TRUE, timeout = 15)

resolve_doi(doi, resolve = TRUE, timeout = 15)

resolve_orcid(orcid, resolve = TRUE, timeout = 15)

resolve_pubmed(pmid, resolve = TRUE, timeout = 15)

resolve_ror(ror, resolve = TRUE, timeout = 15)

resolve_cellosaurus(cvcl, resolve = TRUE, timeout = 15)
}
\arguments{
\item{rrid, doi, orcid, pmid, ror, cvcl}{The identifier to resolve.}

\item{resolve}{Whether to contact the registry (default \code{TRUE}). Set \code{FALSE}
for a purely offline, normalize-only result.}

\item{timeout}{Request timeout in seconds.}
}
\value{
A list with \code{input}, \code{normalized}, \code{resolved}, \code{source}, \code{name},
\code{type}, and \code{url}.
}
\description{
Each resolver normalizes an identifier and, when \code{resolve = TRUE} and the
registry is reachable, retrieves a display name and type. All calls degrade
gracefully: offline or on error, \code{resolved} is \code{FALSE}.
}
\examples{
# Offline: normalize only (no network).
resolve_rrid("RRID:AB_390204", resolve = FALSE)
resolve_doi("10.1038/sdata.2016.18", resolve = FALSE)$normalized
\donttest{
# Live lookup. This contacts Crossref; offline it simply returns the
# normalized identifier with `resolved = FALSE` instead of failing.
resolve_doi("10.1038/sdata.2016.18", resolve = TRUE)$resolved
}
}

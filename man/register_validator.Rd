% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validator_registry.R
\name{register_validator}
\alias{register_validator}
\title{Register a validation rule}
\usage{
register_validator(
  rule_id,
  fn,
  layer = c("structural", "semantic"),
  severity = c("error", "warning", "note", "info"),
  applies = function(x) TRUE,
  standard = NA_character_,
  replace = FALSE
)
}
\arguments{
\item{rule_id}{A unique rule identifier, e.g. \code{"struct-missing-name"}.}

\item{fn}{The rule function \verb{function(x, ctx)} returning a list of issues.}

\item{layer}{Either \code{"structural"} or \code{"semantic"}.}

\item{severity}{Default severity: one of \code{"error"}, \code{"warning"}, \code{"note"},
\code{"info"}.}

\item{applies}{A predicate \verb{function(x)}; the rule runs only when it returns
\code{TRUE} (used by conditional rule packs).}

\item{standard}{Optional reporting standard the rule enforces (e.g.
\code{"ARRIVE-2.0"}).}

\item{replace}{Overwrite an existing rule with the same \code{rule_id}? Defaults to
\code{FALSE}, so a plugin cannot silently replace a built-in rule; pass \code{TRUE} to
deliberately override one.}
}
\value{
Invisibly \code{NULL}; called for its side effect.
}
\description{
Adds a rule to the validation engine. A rule is a function \code{fn(x, ctx)} that
inspects a \link{krt_tbl} and returns a list of issues (each created with the
internal issue helper); the engine attaches the rule id, layer, standard, and
resolved severity.
}
\examples{
# A rule flags resources whose display name is very long.
long_name_rule <- function(x, ctx) list()
# register_validator("demo-long-name", long_name_rule, severity = "note")
head(list_validators(), 3)
}

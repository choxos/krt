% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deposit.R
\name{krt_deposit_zenodo}
\alias{krt_deposit_zenodo}
\title{Deposit a KRT to Zenodo}
\usage{
krt_deposit_zenodo(
  x,
  token = Sys.getenv("ZENODO_TOKEN"),
  sandbox = TRUE,
  metadata = NULL,
  publish = FALSE,
  audience = c("public", "author"),
  timeout = 60
)
}
\arguments{
\item{x}{A \link{krt_tbl}.}

\item{token}{Zenodo access token (defaults to \code{ZENODO_TOKEN}).}

\item{sandbox}{Use the Zenodo sandbox (default \code{TRUE}).}

\item{metadata}{Optional Zenodo metadata list (a sensible default otherwise).}

\item{publish}{Whether to publish immediately (default \code{FALSE}).}

\item{audience}{\code{"public"} (redacted, the default) or \code{"author"} (full).}

\item{timeout}{Request timeout in seconds.}
}
\value{
A list with \code{deposit} (the API response), \code{doi}, and \code{x} (the table
with the DOI recorded). On failure, \code{deposit} is \code{NULL}.
}
\description{
Creates a Zenodo deposition, uploads the table as JSON, sets metadata, and
optionally publishes. The minted (pre-reserved) DOI is written back into the
table's provenance and \code{table_id}. Deposits default to the redacted public
audience, since a deposit is a shared artifact.
}
\examples{
\dontrun{
res <- krt_deposit_zenodo(krt_example, sandbox = TRUE)
res$doi
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_tabular.R
\name{export_tabular}
\alias{export_tabular}
\title{Export a KRT as a delimited table or spreadsheet}
\usage{
export_tabular(
  x,
  path = NULL,
  format = c("csv", "tsv", "xlsx"),
  profile = NULL,
  view = NULL,
  audience = c("author", "public"),
  redact = NULL,
  attribution = TRUE
)
}
\arguments{
\item{x}{A \link{krt_tbl}.}

\item{path}{Output path, or \code{NULL} to return the content (csv/tsv only).}

\item{format}{\code{"csv"}, \code{"tsv"}, or \code{"xlsx"}.}

\item{profile}{Profile whose columns to use (default the wide core view).}

\item{view}{Optional explicit view name.}

\item{audience}{\code{"author"} (full) or \code{"public"} (redacted).}

\item{redact}{Redaction strength for public output, or \code{FALSE} to disable.}

\item{attribution}{If \code{TRUE} (default), a \code{path} is given, and \code{profile}
carries an attribution requirement, write it as a sidecar next to \code{path}.}
}
\value{
The path (invisibly) when written, or the delimited text.
}
\description{
Export a KRT as a delimited table or spreadsheet
}
\examples{
cat(substr(export_tabular(krt_example, format = "csv"), 1, 60))
}

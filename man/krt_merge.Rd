% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge.R
\name{krt_merge}
\alias{krt_merge}
\title{Merge Key Resources Tables}
\usage{
krt_merge(
  x,
  y,
  ...,
  strategy = c("union", "prefer_x", "prefer_y", "manual"),
  by = resource_signature
)
}
\arguments{
\item{x, y, ...}{Tables to merge (\code{x} supplies the result's metadata).}

\item{strategy}{\code{"union"}/\code{"prefer_x"} (x wins conflicts), \code{"prefer_y"} (later
table wins), or \code{"manual"} (x wins but every conflict is recorded).}

\item{by}{A function computing a resource's match key (default
\code{\link[=resource_signature]{resource_signature()}}).}
}
\value{
The merged \link{krt_tbl}.
}
\description{
Combines resources from two or more tables, matching by normalized identity
signature. Matching resources are merged field by field; the \code{strategy}
resolves field-level conflicts. Conflicts are attached to the result as the
\code{"merge_conflicts"} attribute.
}
\examples{
a <- add_resource(new_krt("A"), "Antibody", "Anti-TH", vendor = "Millipore",
                  catalog_number = "AB152", new_or_reuse = "reuse")
b <- add_resource(new_krt("B"), "Dataset", "D", doi = "10.5281/zenodo.1",
                  new_or_reuse = "new")
krt_merge(a, b)
}

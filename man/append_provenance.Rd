% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{append_provenance}
\alias{append_provenance}
\title{Append a provenance entry to a KRT}
\usage{
append_provenance(x, activity, inputs = NULL, outputs = NULL, params = NULL)
}
\arguments{
\item{x}{A \link{krt_tbl}.}

\item{activity}{A short activity label.}

\item{inputs, outputs}{Optional character vectors of input/output identifiers.}

\item{params}{Optional named list of parameters for the activity.}
}
\value{
The \code{krt_tbl} with the entry appended.
}
\description{
Records that an \code{activity} (for example \code{"normalize_ids"}, \code{"import"},
\code{"validate"}, \code{"resolve"}, \code{"export"}) was applied to the table. Called
automatically by the mutating functions; exported so custom pipelines can
record their own steps.
}
\examples{
k <- append_provenance(new_krt("Demo"), "import", params = list(format = "csv"))
length(krt_provenance(k))
}

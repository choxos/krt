% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract_krt}
\alias{extract_krt}
\title{Extract a Key Resources Table from a manuscript}
\usage{
extract_krt(
  input,
  engine = c("regex", "llm"),
  format = NULL,
  profile = "generic",
  llm = NULL,
  resolve = FALSE,
  existing = c("detect", "ignore"),
  title = NULL
)
}
\arguments{
\item{input}{A file path or text string.}

\item{engine}{\code{"regex"} (deterministic, offline) or \code{"llm"} (requires a
configured provider and API key).}

\item{format}{Optional explicit input format.}

\item{profile}{Profile to assign to the extracted table.}

\item{llm}{A \code{\link[=krt_llm]{krt_llm()}} configuration (for the LLM engine).}

\item{resolve}{Whether to resolve identifiers during validation.}

\item{existing}{\code{"detect"} to also parse an embedded KRT, or \code{"ignore"}.}

\item{title}{Optional title for the extracted table.}
}
\value{
A list with \code{krt} (the extracted \link{krt_tbl}), \code{candidates} (the raw
resource candidates), \code{existing_krt} (a parsed embedded table or \code{NULL}),
and \code{report} (a validation report).
}
\description{
Reads a manuscript (PDF, JATS/NISO XML, DOCX, or text), extracts candidate
resources with the deterministic regex engine (default) or an LLM, and
returns them as a validated \link{krt_tbl}. Every result is provenance-stamped
with the engine used.
}
\examples{
res <- extract_krt("Anti-TH (RRID:AB_390204); FIJI (RRID:SCR_002285).")
nrow(as.data.frame(res$krt))
}

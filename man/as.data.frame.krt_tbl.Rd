% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/krt_tbl.R
\name{as.data.frame.krt_tbl}
\alias{as.data.frame.krt_tbl}
\title{Coerce a KRT to a data frame (a rectangular view)}
\usage{
\method{as.data.frame}{krt_tbl}(x, row.names = NULL, optional = FALSE, ..., view = "wide")
}
\arguments{
\item{x}{A \link{krt_tbl}.}

\item{row.names, optional}{Present for consistency with the \code{\link[=as.data.frame]{as.data.frame()}}
generic; not used.}

\item{...}{Ignored.}

\item{view}{The view to produce: \code{"wide"} (the full union of core fields) or
a profile name such as \code{"asap"} (a profile projection). Must be named.}
}
\value{
A data frame with one row per resource.
}
\description{
Produces a rectangular \emph{view} of the table by filling the union of present
fields with \code{NA}. Many-valued fields are collapsed with \code{"; "} in this view;
the lossless representation is the JSON/YAML export.
}
\examples{
k <- add_resource(new_krt("Demo"), "Dataset", "RNA-seq",
                  new_or_reuse = "new", doi = "10.5281/zenodo.123")
as.data.frame(k)
as.data.frame(krt_example, view = "asap")
}

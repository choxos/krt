% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deposit.R
\name{krt_deposit_figshare}
\alias{krt_deposit_figshare}
\title{Deposit a KRT to Figshare}
\usage{
krt_deposit_figshare(
  x,
  token = Sys.getenv("FIGSHARE_TOKEN"),
  sandbox = TRUE,
  metadata = NULL,
  audience = c("public", "author"),
  timeout = 60
)
}
\arguments{
\item{x}{A \link{krt_tbl}.}

\item{token}{Figshare token (defaults to \code{FIGSHARE_TOKEN}).}

\item{sandbox}{Use the Figshare sandbox (default \code{TRUE}).}

\item{metadata}{Optional article metadata.}

\item{audience}{\code{"public"} (redacted, the default) or \code{"author"} (full).}

\item{timeout}{Request timeout in seconds.}
}
\value{
A list with \code{article} (the API response), \code{doi}, \code{uploaded}
(logical), and \code{x}.
}
\description{
Creates a Figshare article, reserves a DOI, and uploads the table as JSON.
The DOI is recorded in the table's provenance. Deposits default to the
redacted public audience.
}
\examples{
\dontrun{
krt_deposit_figshare(krt_example)
}
}

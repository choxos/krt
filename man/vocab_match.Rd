% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab.R
\name{vocab_match}
\alias{vocab_match}
\title{Match a value against a controlled vocabulary}
\usage{
vocab_match(value, vocab, fuzzy = FALSE)
}
\arguments{
\item{value}{A character value to check.}

\item{vocab}{A character vector of allowed values, or the name of a
vocabulary (resolved with \code{\link[=krt_vocab]{krt_vocab()}}).}

\item{fuzzy}{If \code{TRUE}, when there is no exact match return the nearest
vocabulary term (by edit distance) as a suggestion instead of \code{NA}.}
}
\value{
A list with \code{ok} (logical), \code{value} (the matched canonical term or
\code{NA}), and \code{suggestion} (nearest term when \code{fuzzy} and not matched).
}
\description{
Match a value against a controlled vocabulary
}
\examples{
vocab_match("Antibody", "resource_type")
vocab_match("antibodies", "resource_type", fuzzy = TRUE)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_krt}
\alias{validate_krt}
\title{Validate a Key Resources Table}
\usage{
validate_krt(
  x,
  profile = NULL,
  layers = c("structural", "semantic"),
  resolve = FALSE,
  severity = NULL,
  attach = FALSE
)
}
\arguments{
\item{x}{A \link{krt_tbl}.}

\item{profile}{Profile whose severity overrides apply (default the table's
profile).}

\item{layers}{Which layers to run: \code{"structural"}, \code{"semantic"}, or both.}

\item{resolve}{If \code{TRUE}, semantic rules may perform online existence checks
(off by default; never on CRAN).}

\item{severity}{Optional named list mapping \code{rule_id} to a severity (or
\code{"off"}), overriding rule and profile defaults.}

\item{attach}{If \code{TRUE}, return the table with the findings stored in its
\code{validation} slot instead of returning the report.}
}
\value{
A \code{krt_validation_report}, or the \code{krt_tbl} when \code{attach = TRUE}.
}
\description{
Runs the registered validation rules over a table and returns a
\link{krt_validation_report}. Structural rules check schema conformance offline;
semantic rules check cross-field consistency and, when \code{resolve = TRUE},
identifier existence. Conditional packs (cell-line authentication, organism
metadata, ethics/consent) apply only when the relevant resource types are
present; they are minimal checks, not full ICLAC or ARRIVE assessments.
}
\details{
Each finding's severity is resolved from the rule default, then any
profile override, then any per-rule value in \code{severity}. A severity of
\code{"off"} disables the rule.
}
\examples{
validate_krt(krt_example)
validate_krt(krt_example, layers = "structural")
}

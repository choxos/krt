% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggest.R
\name{krt_suggest}
\alias{krt_suggest}
\title{Suggest canonical names and identifiers from public authorities}
\usage{
krt_suggest(query, type = NULL, authority = "auto", n = 10, resolve = TRUE)
}
\arguments{
\item{query}{The text to search for.}

\item{type}{Optional resource type hint.}

\item{authority}{Which authority to query: \code{"auto"}, or the name of any
registered source (built-ins: \code{"taxonomy"}, \code{"cellosaurus"}, \code{"chebi"},
\code{"ror"}; plus any added with \code{\link[=register_suggest_source]{register_suggest_source()}}).}

\item{n}{Maximum number of suggestions.}

\item{resolve}{Whether to contact the network (default \code{TRUE}).}
}
\value{
A data frame with columns \code{label}, \code{id}, \code{authority}, \code{score}, \code{uri}.
}
\description{
Queries ontology and registry search endpoints to autocomplete a resource's
canonical name or identifier. Requires network access; returns an empty
result offline.
}
\examples{
krt_suggest("dopamine", authority = "chebi", resolve = FALSE)
}

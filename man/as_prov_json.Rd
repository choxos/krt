% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rocrate.R
\name{as_prov_json}
\alias{as_prov_json}
\title{Export the provenance graph as PROV-JSON}
\usage{
as_prov_json(x, path = NULL)
}
\arguments{
\item{x}{A \link{krt_tbl}.}

\item{path}{Output path, or \code{NULL} to return the JSON string.}
}
\value{
The JSON string, or the path (invisibly).
}
\description{
Serializes the table's recorded activities as a W3C PROV-JSON document: the
table is an entity, each recorded step is an activity associated with the
\code{krt} software agent and generating the table.
}
\examples{
cat(substr(as_prov_json(normalize_ids(krt_example)), 1, 40))
}
